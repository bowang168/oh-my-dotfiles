# Oracle Linux 9 / 10 — packages to install
# Usage: sudo dnf install $(grep -v '^#' packages.txt | tr '\n' ' ')
#
# On OL10, enable EPEL + CodeReady Builder first:
#   sudo dnf install -y oracle-epel-release-el10
#   sudo dnf config-manager --enable ol10_u1_developer_EPEL ol10_codeready_builder
#
# Not in OL10 repos — install from upstream (see README):
#   eza, git-delta, starship, zoxide

# Shell & terminal
zsh
kitty

# CLI tools
bat
eza
fd-find
fzf
git
git-delta
glow
neovim
ripgrep
starship
tree
zoxide

# System utilities
htop

# Flatpak (GUI app distribution; apps listed in linux/flatpaks.txt)
flatpak

# GNOME
gnome-tweaks

# Window management (for toggle_app on X11; wmctrl only — toggle_app uses
# GNOME "Window Calls" extension on Wayland. xdotool is X11-only and not in
# EPEL10; install it manually if you need the X11 fallback.)
wmctrl

# Build deps (for keyd — install.py builds it from source)
gcc
make

# Key remapping
# keyd is built + installed by install.py's `keyd` step.
# Config lives at linux/keyd/default.conf; deployed to /etc/keyd/default.conf.
