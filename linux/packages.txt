# Oracle Linux 9 — packages to install
# Usage: sudo dnf install $(grep -v '^#' packages.txt | tr '\n' ' ')

# Shell & terminal
zsh
kitty

# CLI tools
bat
eza
fd-find
fzf
git
git-delta
neovim
ripgrep
starship
tree
zoxide

# System utilities
htop
tmux

# Window management (for toggle_app)
xdotool
wmctrl

# Build deps (for keyd — install.py builds it from source)
gcc
make

# Key remapping
# keyd is built + installed by install.py's `keyd` step.
# Config lives at linux/keyd/default.conf; deployed to /etc/keyd/default.conf.
